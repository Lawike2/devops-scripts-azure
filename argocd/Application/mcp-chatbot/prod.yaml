# Production deployment of the MCP Chatbot.
# Uncomment and configure when ready to promote to production.
#
# Prerequisites:
#   - mcp-chatbot-secrets must exist in the mcp-chatbot-prod namespace:
#     kubectl create secret generic mcp-chatbot-secrets \
#       --from-literal=OPENAI_API_KEY=sk-... \
#       -n mcp-chatbot-prod

# apiVersion: argoproj.io/v1alpha1
# kind: Application
# metadata:
#   name: mcp-chatbot-prod
#   namespace: argocd
# spec:
#   project: default
#   source:
#     repoURL: 'https://github.com/oluwaTG/azure-devops-scripts'
#     targetRevision: main
#     path: ./helm/Application/mcp-chatbot
#     helm:
#       valueFiles:
#         - values.yaml
#       parameters:
#         - name: mcpServerUrl
#           value: 'http://mcp-server.mcp-server.svc.cluster.local:8080'
#         - name: openaiSecretName
#           value: mcp-chatbot-secrets
#         - name: replicaCount
#           value: '2'
#         - name: ingress.enabled
#           value: 'true'
#         - name: ingress.hosts[0].host
#           value: 'mcp-chatbot.example.com'
#   destination:
#     server: 'https://kubernetes.default.svc'
#     namespace: mcp-chatbot-prod
#   syncPolicy:
#     automated:
#       prune: true
#       selfHeal: true
#     syncOptions:
#       - CreateNamespace=true
