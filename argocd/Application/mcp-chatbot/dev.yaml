apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mcp-chatbot-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/oluwaTG/azure-devops-scripts'
    targetRevision: main
    path: ./helm/Application/mcp-chatbot
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: mcp-chatbot-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
